/* eslint-disable */
/* eslint-enable */

const state = {
  user: {
    email: "Maksym.Gorbunov@iths.se",
    pass: ""
  }
};

const getters = {
  user: state => state.user
};

const actions = {
  
};

const mutations = {
 
};

export default {
  state,
  getters,
  actions,
  mutations
};
