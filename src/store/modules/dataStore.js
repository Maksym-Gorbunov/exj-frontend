/* eslint-disable */
import axios from "axios";
import usersStore from "./userStore";
/* eslint-enable */

const state = {
  country: {
    name: "Italy"
  }
  
};

const getters = {
  country: state => state.country
};

const actions = {
  
};

const mutations = {
  
};

export default {
  state,
  getters,
  actions,
  mutations
};
